JG is a machine learning software (Python Script) employs LSTM networks to train an LSTM model to learn a data set of 50k English plaintext jokes, and then generate new jokes. Work still in progress.
